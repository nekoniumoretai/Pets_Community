<div class="container">
  <h2>いいねした投稿一覧</h2>
    <% if @favorite_posts.any? %>
      <ul>
        <% @favorite_posts.each do |post| %>
          <li>
            <%= link_to post.title, post_path(post) %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p>いいねした投稿はありません</p>
    <% end %>
    </div>
  </div>
</div>

