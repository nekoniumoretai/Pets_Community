<div class="container">
  <h2>いいねした投稿一覧</h2>
  <p>（投稿タイトルをクリックして詳細が確認できます）</p>
    <% if @favorite_posts.any? %>
      <ul>
        <% @favorite_posts.each do |post| %>
          <li>
            <%= link_to post.title, post_path(post), class: "favorite-link" %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p>いいねした投稿はありません</p>
    <% end %>
    </div>
  </div>
</div>

