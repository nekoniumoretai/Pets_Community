<div class="container">
  <div class="row justify-content-center">
    <h3>フォロー一覧</h3>
    <ul>
      <% @users.each do |user| %>
        <li>
          <%= link_to "#{user.nickname}", user_path(user) %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
